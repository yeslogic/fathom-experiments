type Point {
  x : #Int;
  y : #Int;
  z : #Int;
}

format point :=
  let x <- byte;
  let y <- byte;
  let z <- byte;
  #pure Point (Point { x := x; y := y; z := z });

format repeat-pure := #repeat-len 3 (#pure #Int 42);

def my-point : Point := Point { x := 1; y := 2; z := 3 };
