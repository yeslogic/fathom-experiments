type point-record {
  x : #Int;
  y : #Int;
  z : #Int;
}

format point :=
  let x <- byte;
  let y <- byte;
  let z <- byte;
  #pure point-record (point-record { x := x; y := y; z := z });

def my-point : point-record = point-record { x := 1; y := 2; z := 3 };
