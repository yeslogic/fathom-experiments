(executable
  (name Main)
  (public_name ll1-dsl2)
  (package ll1-dsl2)
  (preprocess
    (pps sedlex.ppx))
  (libraries
    byte-set
    menhirLib
    sedlex
    tsort))

(menhir
  (modules Parser)
  (flags --explain --strict))
